fun main() {
  arr := [5, 1, 4, 2, 8]
  n := arr.length
  i := 0
  while i < n {
    j := 0
    while j < n - i - 1 {
      if arr[j] > arr[j + 1] {
        tmp := arr[j]
        arr[j] = arr[j + 1]
        arr[j + 1] = tmp
      }
      j = j + 1
    }
    i = i + 1
  }
  print(arr)
}
